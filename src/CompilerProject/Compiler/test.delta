@extern
print :: (x : i32);

sum :: (x : i32, y : i32, z : i32) -> i32 {
    ret = x + y;
}

double :: (x : i32) -> i32 {
    ret = x * 2;
}

main :: () {
    print(10);

    x := sum(10, 5);
    
    loop x < 50 {
        x = x + 1;
    }
}